# @package _global_
defaults:
  - override /data: flickr8
  - override /lr_scheduler: steplr

model:
  alpha_regularization: 0

lr_scheduler:
  step_size: 1

optimizer:
  lr: 1E-2
  weight_decay: 1E-6

trainer:
  max_epochs: 5

